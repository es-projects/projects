<%= form_with(model: project_application, local: true) do |form| %>
  <% if project_application.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(project_application.errors.count, "error") %> prohibited this project_application from being saved:</h2>

      <ul>
        <% project_application.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="form-group">
    <%= form.label t(:group) %>
    <%= form.number_field :group_id, :class=>"form-control" %>
  </div>

  <div class="form-group">
    <%= form.label t(:project) %>
    <%= form.number_field :project_id, :class=>"form-control" %>
  </div>

  <div class="form-group">
    <%= form.label t(:accepted) %>
    <%= form.check_box :accepted, :class=>"form-control" %>
  </div>

  <div class="form-group">
    <%= form.submit (form.object.new_record? ? t(:create_project_application) : t(:update_project_application)), :class => "btn btn-primary" %>
  </div>
<% end %>
